[package]
name = "finvoice-tehtava"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.45"
flexpdf = "0.1.6"
quick-xml = { version = "0.37", features = ["serialize"]}
serde = { version = "1", features = ["derive"]}
